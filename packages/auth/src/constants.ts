/** Cookie name used by Payload CMS for JWT tokens */
export const COOKIE_NAME = 'payload-token'

/** Default PATHS API URL (direct to Render, not via gateway) */
export const DEFAULT_PATHS_API_URL = 'https://paths-api.onrender.com/learn'

/** Cookie domain for cross-service auth */
export const COOKIE_DOMAIN = '.limitless-longevity.health'
